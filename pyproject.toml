[project]
name = "pii-detector"
version = "0.1.0"
description = "Stateless PII span detection service (regex + GLiNER2) for pre-LLM tokenization pipelines"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"
