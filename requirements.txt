# Core app dependencies. The ML stack lives in requirements-model.txt so tests
# and CI never have to install torch.
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
pydantic>=2.7,<3
phonenumbers>=8.13,<10
PyYAML>=6.0.3,<7
